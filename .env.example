# Admin girişi (/admin). Bu dosyayı kopyalayıp .env.local yapın:
#   cp .env.example .env.local
# Şifreyi istediğiniz gibi değiştirin, sonra dev sunucuyu yeniden başlatın (npm run dev).

VITE_ADMIN_PASSWORD=
